# HGPF導向 AI-RAG 臺灣客家族譜書寫系統設計

## 1. 系統定位

本系統是「證據先於敘事」的研究與編修雛型。它把族譜、戶籍、契約、墓碑、地方志與訪談中的資訊轉為可追溯主張，主動搜尋支持與反證，再由具名研究者決定哪些內容可寫入族譜。

系統是 GPS-aligned，而非 GPS 認證工具。檢索分數、OCR可用性、抽取信心與史料可信度是四個不同概念，不可互相替代。

核心原則：

1. 原文不被覆蓋。
2. 異說不被消音。
3. 推論不冒充事實。
4. 相似度不冒充證據權重。
5. 人工決定留下責任軌跡。

## 2. 需求追溯

| HGPF要求 | 系統實作 | 目前邊界 |
|---|---|---|
| M1 描述層 | Document、Passage、31項在地欄位候選、原文與異體保存 | 尚未完成版面座標與逐字校訂介面 |
| M2 證據層 | SHA-256、來源路徑、版本metadata、頁碼／段落定位、ProcessingActivity | Markdown無原頁標記時只能定位至段落 |
| M3 推論／稽核層 | Claim、EvidenceRelation、混合檢索、反證排序、GPS五項稽核 | 圖關係檢索與自動ConflictSet為下一階段 |
| M4 治理層 | access_level、具名覆核、草稿狀態、處置理由、活動軌跡 | 尚未實作登入、角色型強制授權與欄位遮蔽 |
| 證據約束書寫 | 只用已掛接證據卡；支持／反駁／限制／脈絡分段；保留限定語 | 目前為規則式本機基線，不宣稱大型LLM生成能力 |
| 適當棄答 | 無支持證據、未解衝突、低品質OCR或受限資料時標示Audit-flagged | 發布閘門仍需強化為後端不可繞過的政策 |

上傳文件可先執行「文件預檢」：HGPF 31項只標示候選命中、未找到或可能不適用；GPS五項只評估研究就緒度。預檢不會把欄位命中當成可信度，也不會把單一文件宣告為完成GPS證明。

## 3. 整體架構

```text
族譜影像／OCR／DOCX／PDF
          │
          ▼
來源建檔與結構切段 ── SHA-256、頁碼、文類、OCR可用性
          │
          ▼
HGPF知識基礎 ── Document ─ Passage ─ Claim ─ EvidenceRelation
          │                         │
          ├── 全文檢索             ├── 支持
          ├── 字元向量             ├── 反駁
          ├── HGPF欄位             ├── 限制
          └── 反證詞／OCR訊號       └── 脈絡
          │
          ▼
GPS五項稽核 ── 研究範圍、引用、關聯、衝突、證明文字
          │
          ▼
證據約束書寫 ── 證據狀態、逐項引文、OCR／存取警示
          │
          ▼
人工覆核與發布狀態 ── reviewer、reason、timestamp、activity log
```

## 4. 核心資料模型

### 4.1 已實作實體

- `documents`：來源物件、雜湊、類型、存取層級與文件metadata。
- `passages`：結構化切段、頁碼提示、字元位置、HGPF欄位候選、檢索向量、OCR可用性。
- `claims`：可支持或反駁的最小研究主張，含主體、類型、HGPF欄位與人工信心標籤。
- `evidence_links`：主張與片段間的支持、反駁、限制或脈絡關係。
- `research_events`：查詢式、模式、過濾範圍、索引版本與結果數。
- `audit_runs`：GPS五項分項發現、待辦與非認證聲明。
- `drafts`：證據約束草稿、引文卡、覆核狀態與具名決策。
- `processing_activities`：匯入、掛證、生成與覆核的PROV式活動軌跡。

### 4.2 下一階段實體

- `persons`、`name_forms`、`kinship_assertions`：處理一人異名、同名異人與多型親屬。
- `migration_legs`：出發地、中轉地、到達地、時間與證據分開建模。
- `identity_claims`、`language_use_claims`：把客家身分與腔調保存為具主張者、時空與來源的事件性主張。
- `ancestral_sites`、`feng_shui_claims`：分開地點事實、坐向原文、正規化與信仰性解釋。
- `conflict_sets`：讓多個版本異說形成可保留、可處置、不可靜默覆蓋的集合。
- `research_plans`、`expected_sources`：使GPS1由「查了幾次」提升為「預定來源是否查畢」。
- `draft_sentences`：每句草稿直接連結Claim與Citation，支援逐句稽核。

## 5. 混合RAG設計

檢索採四種訊號合成候選排序：

1. 全文／表面詞：人名、地名、堂號、字輩、年號與特殊用字。
2. 中文雙字元雜湊向量：提供可重現的本機語意基線。
3. HGPF欄位訊號：依研究問題聚焦源流、親屬、語言、風水等內容。
4. 反證與品質訊號：提高「異說、失考、不符、或云」等片段的可見性，並降低OCR可用性差的片段排序。

排序分數只表示候選相關性。使用者仍須把片段明確標為支持、反駁、限制或脈絡；系統不得由top-k名次自動宣告支持。示範資料只有在實體、地點與屬性錨點同時出現時才建立候選支持關係。

## 6. 證據狀態與語氣控制

| 證據狀態 | 判定基線 | 允許語氣 |
|---|---|---|
| 支持 | 至少兩份支持來源，且無未處理重大衝突 | 「現有資料支持」「在揭露範圍內可確認」 |
| 條件支持 | 有支持證據，但僅單一文件／版本或適用範圍有限 | 「本版本記載」「在目前來源範圍內」 |
| 衝突 | 有反駁／限制證據且尚無具名處置 | 必須並陳異說，不得選定一說 |
| 證據不足 | 沒有支持證據 | 「待考」「尚無法判定」 |
| 限制存取 | 使用宗族限定或敏感來源 | 不得直接轉為公開版本 |

人工輸入「已證」不能覆蓋系統計算出的衝突或證據不足狀態。低OCR可用性片段必須提示回看影像；品質分數不得被解讀為來源可信度。

## 7. GPS五項操作化

1. GPS1：保存每次混合與反證查詢；顯示來源與類型覆蓋，永遠保留「研究者界定合理廣泛範圍」。
2. GPS2：檢查文件、段落與原頁定位。只有段落定位時不得給滿分。
3. GPS3：檢查證據關係類型、HGPF欄位與OCR可用性；來源品質由人判斷。
4. GPS4：反駁／限制出現時，要求具名處置理由；AI不能自行結案。
5. GPS5：草稿必須附證據卡；Audit-flagged或未具名覆核不得視為可發布結論。

## 8. 狀態機

```text
Draft
  └─有可定位證據→ Evidence-linked
       ├─缺漏／衝突／OCR／權限風險→ Audit-flagged
       │      └─補證或人工處置→ Human-reviewed
       └─具名人工覆核→ Human-reviewed
              ├─核定特定版本與權限→ Approved-for-publication
              └─仍不足→ Needs-further-research ──回到檢索與研究計畫
```

後端禁止模型直接設定Human-reviewed或Approved-for-publication。發布版須另加入M4強制授權、在世者遮蔽、敏感墓址降精度與撤回機制。

## 9. 評估設計

比較四種條件：LLM-only、一般向量RAG、HGPF混合RAG、HGPF混合RAG＋GPS稽核＋人工覆核。至少評估：

- 檢索：Recall@k、反證Recall@k、來源多樣性、頁面可定位率。
- 忠實性：引用支持率、限定語保存率、無來源句率。
- 譜系任務：同名誤併率、親屬類型F1、遷徙鏈完整率、衝突偵測F1。
- 在地風險：祖籍誤推客家身分率、祖籍誤推腔調率、風水因果誤寫率。
- 人機協作：適當棄答率、專家查核時間、修正次數、信任校準與認知負荷。

## 10. 部署邊界

現版是單機研究雛型，使用FastAPI、SQLite與純前端介面，預設僅監聽127.0.0.1。若進入多人或公開服務，必須先完成：帳號與角色權限、受限片段在檢索前過濾、加密備份、不可竄改稽核紀錄、模型與索引版本登錄、影像區塊引用、資料撤回與宗族／提供者同意流程。
